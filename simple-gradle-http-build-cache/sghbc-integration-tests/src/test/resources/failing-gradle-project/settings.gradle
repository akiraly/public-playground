import org.gradle.caching.http.HttpBuildCache

rootProject.name = 'failing-gradle-project'

// Build cache configuration
buildCache {
    local {
        enabled = false
    }
    remote(HttpBuildCache) {
        url = 'http://localhost:8123/cache/'
        enabled = true
        push = true
        allowInsecureProtocol = true
    }
}
